# Make your mark

*2026-07-02 · shell-challenge · progression track · +25 XP · `sq-02-make`*

## The problem

In your lab, create three things: a folder called workshop, an empty file workshop/notes.txt, and a file name.txt containing exactly the word ready. Useful commands: mkdir NAME (make a folder), touch FILE (make an empty file), echo text > FILE (make a file containing text).

## How I did it

I used ``` mkdir NAME ``` which made a folder. Then I used ``` touch FILE ``` for the empty, and ```echo text > FILE ```

## Verified against

- `workshop exists` ✓
- `workshop/notes.txt exists` ✓
- `name.txt contains /^ready$/` ✓

## Reflection

I think I made a similar mistake to the last one and not understanding clearly what the lesson was asking me. I believe I had been staring at a screen for way too long.
