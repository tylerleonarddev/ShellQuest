# Build the nest

*2026-07-04 · shell-challenge · progression track · +30 XP · `sq-03-tree`*

## The problem

Folders can live inside folders inside folders — and mkdir -p builds a whole chain in one shot (mkdir -p a/b/c). In your lab, create the structure project/src/utils/ plus an empty file project/src/main.txt.

## How I did it

I used mkdir -p a/b/c to build the directory and then cd to the src dir.``` Followed by touch FILE ``` 

## Verified against

- `project/src/utils exists` ✓
- `project/src/main.txt exists` ✓

## Reflection

This one I felt was more easy than the last the last two because I had a clear mind and I could read the instructions properly. :D
